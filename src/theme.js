import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#3CCE88',
            contrastText: '#FFFFFF'
        },
        secondary: {
            main: '#002C65',
            contrastText: '#FFFFFF'
        }
    }
})

export default theme;